[package]
name = "cosplay-generator-tests"

edition.workspace = true
version.workspace = true

[dependencies]
cosplay-codec.workspace = true

[build-dependencies]
cosplay-generator = { workspace = true, features = ["serde"] }
cosplay-xml.workspace = true

toml = { workspace = true, features = ["parse", "serde"] }

[dev-dependencies]
tokio = { workspace = true, features = ["io-util", "macros", "rt-multi-thread"] }
