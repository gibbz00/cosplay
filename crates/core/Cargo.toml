[package]
name = "async-wayland-core"

edition.workspace = true
version.workspace = true

[dependencies]
rustix = { workspace = true, features = ["net"] }

# TEMP:
tokio = { version = "1", features = ["net"] }
